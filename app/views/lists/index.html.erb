<h1>Lists</h1>


<p>  id name</p>
<ol>
  <% @lists.each do |list| %>
  <div>
    <h3><%= link_to list.name, list_path(list) %></h3>
  </div>
  <% end %>
</ol>
<br>
<%= link_to 'Add List', new_list_path(@list)  %>
